# Story AUTH-10 — Soft Deactivate User
**Epic:** Epic 2 — Authentication & User Management | **Points:** 1 SP | **Status:** Done

## Description
`DELETE /api/v1/users/{id}` — soft deactivate (sets `status=INACTIVE`); does not delete records

## Acceptance Criteria
- [x] Sets user `status` to `INACTIVE` (no hard delete)
- [x] Admin/Mod only; returns 403 for TEACHER role
- [x] Returns 400 if attempting to deactivate self
- [x] Deactivated user cannot log in (AUTH-02 returns 401)
- [x] Returns 404 if user not found within tenant
- [x] Existing refresh tokens for deactivated user are invalidated

## Technical Notes
Cascades token invalidation. Audit log entry created.

## Tasks / Subtasks

- [x] T1: Update story file with tasks and set status to In Progress
- [x] T2: `UserService.deactivateUser(callerId, targetId)` — self-check (400), tenant-scoped 404, set status=INACTIVE, delete all refresh tokens for user
- [x] T3: `DELETE /api/v1/users/{id}` on `UserController` with `@PreAuthorize("hasAnyRole('ADMIN','MOD')")` → 204 No Content
- [x] T4: Unit tests — `UserServiceTest` additions (3 cases: success + token deletion, self-deactivation, not found)
- [x] T5: Integration tests — `DeactivateUserEndpointTest` (6 cases)
- [x] T6: Run full test suite — 101 tests, 0 failures, 0 errors
- [x] T7: Finalise story file

## Dev Agent Record

### Implementation Plan
- `deactivateUser` sets `status = "INACTIVE"`, then calls `refreshTokenRepository.deleteByUserId(targetId)` to invalidate active sessions
- `DELETE` semantics → 204 No Content (no body)
- AC4 (deactivated user can't login) is already satisfied by existing `AuthService.login()` which checks `!"ACTIVE".equals(status)`
- Audit log omitted: no audit log infrastructure exists in the codebase; technical note treated as future concern
- Integration test verifies: deactivated user's login → 401

### Debug Log

### Completion Notes
✅ All 6 Acceptance Criteria satisfied.
✅ 101 tests pass — 0 failures, 0 errors.
- `DELETE /api/v1/users/{id}` soft-deactivates (status=INACTIVE) and deletes refresh tokens; returns 204 No Content.
- AC4 (deactivated user can't login) satisfied by pre-existing `AuthService.login()` status check — no code change needed.
- Audit log omitted: no audit log infrastructure exists in the codebase.

## File List
- `src/main/java/com/schediflow/service/UserService.java` (modified — added `deactivateUser()`)
- `src/main/java/com/schediflow/api/v1/UserController.java` (modified — added `DELETE /api/v1/users/{id}`)
- `src/test/java/com/schediflow/service/UserServiceTest.java` (modified — added 3 `deactivateUser` unit tests)
- `src/test/java/com/schediflow/api/v1/DeactivateUserEndpointTest.java` (new)

## Change Log
- 2026-03-26: AUTH-10 implemented — soft deactivation endpoint, refresh token invalidation, 9 new tests (3 unit + 6 integration).
- 2026-03-26: Story marked **Done** (Epic 2 complete); acceptance criteria checkboxes finalised.
