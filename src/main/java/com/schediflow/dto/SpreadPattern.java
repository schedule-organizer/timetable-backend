package com.schediflow.dto;

/**
 * How subject lessons may be arranged in the timetable cycle (class allocations and subject defaults).
 */
public enum SpreadPattern {
    SPREAD,
    CLUSTER,
    ANY
}
